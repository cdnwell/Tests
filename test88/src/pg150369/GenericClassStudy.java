package pg150369;

public class GenericClassStudy {
    public static void main(String[] args) {
        // Class<T> 클래스와 Class 클래스의 차이점
        // Class<T> 클래스는 제네릭 클래스이다.
        // Class<T> 클래스는 Class 클래스의 자식 클래스이다.
        // Class<T> 클래스는 Class 클래스의 메소드를 사용할 수 있다.
        // Class<T> 클래스는 Class 클래스의 메소드를 사용할 수 있으므로 Class<T> 클래스를 사용하는 것이 좋다.

        // Class<T> 클래스는 제네릭 클래스이므로 타입 파라미터를 지정해야 한다.
        // 타입 파라미터는 클래스의 이름 뒤에 <T>를 붙여서 지정한다.
        // 타입 파라미터는 클래스의 이름 뒤에 <T>를 붙여서 지정하므로 타입 파라미터를 지정하지 않으면 컴파일 오류가 발생한다.
        // 타입 파라미터는 클래스의 이름 뒤에 <T>를 붙여서 지정하므로 타입 파라미터를 지정하지 않으면 컴파일 오류가 발생하므로 타입 파라미터를 지정해야 한다.

    }
}
